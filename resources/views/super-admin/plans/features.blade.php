@extends('super-admin.layout') @section('title','Feature Catalogue') @section('content')
<div class="page-header"><div><h1 class="page-title">Feature Catalogue</h1><p>Stable authorization codes and controlled icon keys.</p></div><a class="btn btn-outline" href="{{route('super-admin.plans.index')}}">Plans</a></div>@if(session('success'))<div class="alert alert-success">{{session('success')}}</div>@endif
@foreach($features as $feature)<form method="POST" action="{{route('super-admin.plans.features.update',$feature)}}" class="card" style="margin-bottom:1rem">@csrf @method('PUT')<div class="card-body grid grid-3"><div><label>Stable code</label><code>{{$feature->code}}</code></div><div class="form-group"><label>Name</label><input name="name" value="{{$feature->name}}"></div><div class="form-group"><label>Icon</label><select name="icon">@foreach($icons as $icon)<option @selected($feature->icon===$icon)>{{$icon}}</option>@endforeach</select></div><div class="form-group"><label>Description</label><input name="description" value="{{$feature->description}}"></div><div class="form-group"><label>Category</label><input name="category" value="{{$feature->category}}"></div><div class="form-group"><label>Order</label><input name="display_order" value="{{$feature->display_order}}"><input type="hidden" name="is_active" value="0"><label><input type="checkbox" name="is_active" value="1" @checked($feature->is_active)> Active</label></div><div><button class="btn btn-primary btn-sm">Save Feature</button></div></div></form>@endforeach
@endsection
