@extends('layouts.marketing') @section('content')<section class="page-hero"><div class="marketing-container"><span class="eyebrow-pill">LensPic journal</span><h1>{{ $title }}</h1><p>{{ $description }}</p></div></section><section class="marketing-section"><div class="marketing-container"><form class="mb-10 flex flex-wrap gap-3" action="{{ route('marketing.blog') }}" method="get"><label class="sr-only" for="blog-search">Search articles</label><input class="field max-w-md" id="blog-search" name="q" value="{{ request('q') }}" placeholder="Search articles"><select class="field max-w-xs" name="category"><option value="">All categories</option>@foreach($categories as $category)<option @selected(request('category')===$category)>{{ $category }}</option>@endforeach</select><button class="button button-primary">Search</button></form><div class="card-grid">@forelse($posts as $post)<article class="marketing-card"><span class="text-xs font-bold uppercase tracking-wider text-indigo">{{ $post['category'] }}</span><h2 class="mt-3">{{ $post['title'] }}</h2><p>{{ $post['excerpt'] }}</p><p class="text-xs">{{ $post['author'] }} · {{ \Carbon\Carbon::parse($post['published_at'])->format('j M Y') }}</p><a class="card-link" href="{{ route('marketing.blog.article',$post['slug']) }}">Read article</a></article>@empty<p>No published articles match your search.</p>@endforelse</div></div></section>@endsection
