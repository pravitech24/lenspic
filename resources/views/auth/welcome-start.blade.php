@extends('layouts.app')
@section('title','Welcome')
@section('content')<div class="start"><div><h1>Welcome, {{ $user->first_name }}!</h1><p>Start by joining an existing group or creating a photo group to share and organise memories effortlessly.</p><div class="actions"><a href="{{ route('groups.join-code') }}" class="action {{ $user->account_type==='user'?'primary':'' }}"><b>🔢 Join a Group</b><span>Enter an event code or use a shared invitation link.</span></a>@if($user->account_type==='photographer' || $user->isAdmin())<a href="{{ route('groups.create') }}" class="action primary"><b>✨ Create a Group</b><span>Create a photo group for your event and invite participants.</span></a>@endif</div></div></div>@endsection
@push('styles')<style>.start{min-height:calc(100vh - 60px);display:grid;place-items:center;padding:24px;background:linear-gradient(135deg,#eef2ff,#fdf2f8)}.start>div{width:min(100%,820px);text-align:center}.start h1{font-size:36px}.start p{color:var(--muted);font-size:16px;margin:8px auto 30px;max-width:650px}.actions{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:18px}.action{display:flex;flex-direction:column;text-align:left;padding:26px;border:2px solid var(--border);background:#fff;border-radius:18px;text-decoration:none;color:var(--text);box-shadow:var(--shadow)}.action.primary{border-color:var(--p)}.action b{font-size:19px;margin-bottom:8px}.action span{color:var(--muted)}</style>@endpush
